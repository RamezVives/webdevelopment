const setup = () => {

    // paragraaf opzoeken
    let paragraaf = document.getElementById("abc");

    // breakpoint hier zetten!
    console.log(paragraaf);

};

window.addEventListener("load", setup);